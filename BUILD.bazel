# ===================================================================
# Copyright © 2025 Sackey Ezekiel Etrue
# Distributed under the terms of the MIT license.
# ===================================================================

load("@rules_cc//cc:cc_library.bzl", "cc_library")
load("@rules_license//rules:license.bzl", "license")

#===========================
# Build C++ Library        #
#===========================

cc_library(
	name = "mytoml",
	srcs = [
		"src/mytoml.cpp",
	],
	hdrs = glob(["include/mytoml/*.hpp"]),
	visibility = ["//visibility:public"],
	includes = ["include"],
)

#===========================
# Build C Library          #
#===========================

cc_library(
	name = "mytomlc",
	srcs = [
		"src/mytomlc.cpp",
	],
	hdrs = glob(["include/mytoml/*.h", "include/mytoml/*.hpp"]),
	visibility = ["//visibility:public"],
    includes = ["src/"],
    linkstatic = True,
	includes = ["include"],
    deps = [":mytoml"],
)

# Packaging – create a zip with headers and libraries
genrule(
	name = "dist_zip",
	srcs = ["README.md", "LICENSE"],
	outs = ["mytoml-${PROJECT_VERSION}.zip"],
	cmd = "zip -r $@ include src README.md LICENSE || true",
)

# ===================================================================
